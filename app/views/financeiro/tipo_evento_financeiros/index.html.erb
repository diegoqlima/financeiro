<h1>Listing tipo_evento_financeiros</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Descricao</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_evento_financeiros.each do |tipo_evento_financeiro| %>
      <tr>
        <td><%= tipo_evento_financeiro.id %></td>
        <td><%= tipo_evento_financeiro.descricao %></td>
        <td><%= link_to 'Show', tipo_evento_financeiro %></td>
        <td><%= link_to 'Edit', edit_tipo_evento_financeiro_path(tipo_evento_financeiro) %></td>
        <td><%= link_to 'Destroy', tipo_evento_financeiro, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo evento financeiro', new_tipo_evento_financeiro_path %>
